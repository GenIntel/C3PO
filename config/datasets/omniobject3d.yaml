name: omniobject3d
class_name: OmniObject3D

modalities:
  - 'rgb'
  - 'category'
  - 'size'
  - 'mask'

path_raw: ${platform.path_datasets}/OmniObject3D
path_preprocess: ${platform.path_datasets}/OmniObject3D_Preprocess

setup_remove_previous: False
setup_override: False

preprocess_meta_remove_previous: False
preprocess_meta_override: False

credentials:
  opendatalab: ${credentials.opendatalab}

#dict_nested_frames:
#    test:
#        car:

#categories:
#    - 'car'

