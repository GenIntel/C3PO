# @package _global_
ablation_name: no_multiview_bs8
test_datasets.co3d:
  subset_fraction: 0.1
method:
    nemo:
      multiview:
        type: single # single multiview multiview-mean-se3
        batch_size: 8

      inference:
        live: False
        sim_feats_mesh_with_image: 'rendered' # 'rendered', 'verts2d'

train: False
test: True
