# @package _global_

ablation_name: dinov2_convex1000

mesh_name: 'convex1000' # 'convex500', 'voxel500' 'poisson500', 'alpha500'

train_datasets:
  labeled:
    mesh_name: ${mesh_name}
    preprocess:
      mesh:
        enabled: True
test_datasets:
  co3d:
    mesh_name: ${mesh_name}
    preprocess:
      mesh:
        enabled: True