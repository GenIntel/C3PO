# @package _global_

method:
  nemo:
    train:
      bank_feats_update: loss_gradient # loss_gradient, normalize_loss_gradient, moving_average, loss
      dense_loss: False
      loss: cross_entropy_smooth # cross_entropy, cross_entropy_smooth, negative_sim
      geodesic_prob_sigma: 0.1
