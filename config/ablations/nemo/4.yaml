# @package _global_
ablation_name: lr5e-5_bs24
method:
    nemo:
        train:
          optimizer:
              lr: 5e-5 # 0.0001
          batch_accumulate_to_next_step: 2
          dataloader:
              batch_size: 12
