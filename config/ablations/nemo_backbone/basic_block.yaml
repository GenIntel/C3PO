# @package _global_
ablation_name: resnet_basic_block_dim_128_stride_1

method:
    nemo:
      model:
        backbone:
          class_name: ResNet
          layers_returned: [ 4, 3, 2 ] # always start with depest layer...
        head:
          class_name: ResNet
          block_type: 'basic' # 'bottleneck', 'basic'
          conv_blocks:
            out_dims: [ 128 ]
            strides: [ 1 ]


train: True
test: True
