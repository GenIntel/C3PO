# @package _global_
ablation_name: resnet_imagenet1k_v1

method:
    nemo:
      model:
        backbone:
          class_name: ResNet
          weights: 'imagenet1k_v1' # 'imagenet1k_v2' 'imagenet1k_v1' 'none'
          freeze: False
          layers_returned: [ 4, 3, 2 ] # always start with depest layer...
        head:
          class_name: ResNet
          block_type: 'bottleneck' # 'bottleneck', 'basic'
          conv_blocks:
            out_dims: [ 128 ]
            strides: [ 1 ]


train: True
test: True
